# Core
torch>=2.0
numpy>=1.23
pyyaml>=6.0
requests>=2.28
tqdm>=4.64

# Optional (uncomment as needed)
# openmm>=8.0        # stage4 MD relaxation
# ray>=2.7           # pipeline parallelism
# viennarna>=2.6     # stage1 ViennaRNA folding (has C fallback)
